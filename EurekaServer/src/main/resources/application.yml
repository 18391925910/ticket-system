server:
  port: 6001
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:6001/eureka/
spring:
  application:
    name: eureka